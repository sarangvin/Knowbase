# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Secrets — never commit API keys/tokens
.env
.env.*
!.env.example

# Vercel
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Throwaway debug/verification scripts (_pool.mjs, _rt.mjs, ...). These are
# scratch tools for probing the live database or API; three times now an
# over-broad `git add -A` has swept one into a commit.
_*.mjs
_*.ts
